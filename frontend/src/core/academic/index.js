/**
 * DegreeFlow Academic Engine
 * --------------------------
 * Public API for the Academic Engine.
 *
 * Other parts of the application should import only from this file.
 * Internal modules (calculators, matchers, loaders, etc.) remain private
 * and are used only by the engine itself.
 */

export { runAcademicEngine } from "./academicEngine";